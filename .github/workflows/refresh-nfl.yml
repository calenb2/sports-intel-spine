name: refresh-nfl

on:
  workflow_dispatch: {}
  schedule:
    - cron: "30 9 * * *"   # nightly; offseason runs are cheap no-ops

concurrency:
  group: spine-write
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - name: Distill
        run: python scripts/distill_nfl.py
      - name: Completeness guard
        run: |
          python - <<'PY'
          import json, sys
          m = json.load(open("distillates/nfl/meta.json"))
          print(m)
          if m["teams_profiled"] < 32: sys.exit("expected >= 32 teams for nfl")
          PY
      - name: Commit
        run: |
          git config user.name "spine-bot"
          git config user.email "spine-bot@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "refresh: nfl distillates $(date -u +%F)"
          git pull --rebase origin main
          git push || (git pull --rebase origin main && git push)
